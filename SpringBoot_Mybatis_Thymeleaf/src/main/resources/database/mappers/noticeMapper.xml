<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
  <mapper namespace="com.ee.y1.board.notice.NoticeMapper">
  	
  	<sql id="searcher">
 
  		<!-- 공통으로 사용하는 sql문의 조각/모음 -->	
  		<where>
				<choose>
				
					<when test="kind == 'Writer'">
						<!-- 문자열을 하나로 합쳐주는 concat()함수 -->
						writer like concat('%', #{search}, '%')
					</when>
					
					<when test="kind == 'Contents'">
						<!-- 문자열을 하나로 합쳐주는 concat()함수 -->
						contents like concat('%', #{search}, '%')
					</when>
					
					<otherwise>
						title like concat('%', #{search}, '%')
					</otherwise>
					
				</choose>
			</where>	
  	
  	</sql>
  	
  
  	<select id="getList" resultType="NoticeVO" parameterType="Pager">
			select * from notice 
			
			<include refid="searcher"></include>
			
			order by num desc
			limit #{startRow}, #{perPage}
			<!-- limit 시작index번호(0부터시작), 갯수 --> 
   		</select>
  	
  	<select id="getTotalCount" parameterType="Pager" resultType="Long">
  		select count(num) from notice  		
  		<include refid="searcher"></include>
  	</select>
  	
  	<select id="getSelect" parameterType="BoardVO" resultType="NoticeVO">
  		select * from notice where num = #{num}
  	</select>
  	
  	<insert id="setInsert" parameterType="BoardVO">
   			insert into notice values(#{num}, #{title}, #{writer},#{contents}, now(), 0)
   	</insert>
   
  
  </mapper>